# ============================================================
#  EEG Acquisition System — Python Dependencies
#  NIT Tiruchirappalli | Dept. of ICE | Guide: Prof. V. Sridevi
#
#  Install with:
#      pip install -r requirements.txt
#
#  Python 3.10 or higher required.
#  Tested on Python 3.10 / 3.11 / 3.12
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
starlette>=0.37.0
python-multipart>=0.0.9       # Required for FastAPI file uploads (UploadFile)

# ── WebSocket support ─────────────────────────────────────────
websockets>=12.0

# ── EEG / Signal Processing ──────────────────────────────────
mne>=1.7.0                    # EDF file loading and export
scipy>=1.12.0                 # IIR filter design (butter, cheby1, cheby2, ellip, iirnotch, filtfilt)
numpy>=1.26.0                 # Array operations

# ── USB / Serial Communication ────────────────────────────────
pyserial>=3.5                 # Serial port access for USB EEG device streaming

# ── Firebase (USB streaming transport layer only) ─────────────
firebase-admin>=6.5.0         # Firebase Admin SDK — writes EEG chunks to Realtime Database
                              # Not needed if you only use File (EDF) mode

# ── Data Validation ───────────────────────────────────────────
pydantic>=2.6.0               # Used internally by FastAPI

# ── Optional: Export dependencies (already included via mne) ──
# edfio                       # mne uses this internally for EDF export — installed with mne
